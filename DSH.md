# DSH.md (cc-import project)

This document is the project memory file of DeepSeek Harness (DSH),  
generated by the `/init` command of this plugin (or maintained manually), and is loaded into the model context during session assembly.

This project is cc-import — a plugin that imports Claude Code memories and conversations into DeepSeek Harness.

- Written in TypeScript, built using tsdown.
- Code comments are primarily in Chinese.
- Package name/plugin ID: `cc-import` (host-side: `dsh.client`, client-side: partial).

@docs/conventions.md
